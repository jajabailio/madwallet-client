#!/usr/bin/env sh

echo "🔍 Running pre-push checks for client..."

# Type check
echo "\n📝 Type checking..."
npx tsc --noEmit
if [ $? -ne 0 ]; then
  echo "❌ Type check failed"
  exit 1
fi

# Build
echo "\n🏗️  Building..."
yarn build
if [ $? -ne 0 ]; then
  echo "❌ Build failed"
  exit 1
fi

# Run tests
echo "\n🧪 Running tests..."
yarn test
if [ $? -ne 0 ]; then
  echo "❌ Tests failed"
  exit 1
fi

echo "\n✅ All pre-push checks passed!"
