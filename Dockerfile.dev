# Development Dockerfile for Client
FROM node:22-alpine

WORKDIR /app

# Install dependencies at startup if needed
CMD sh -c "if [ ! -d 'node_modules' ] || [ ! -d 'node_modules/vite' ]; then yarn install; fi && yarn dev --host 0.0.0.0"
